<h1>Topics</h1>

<div class="row">
   <div>
     <%= render "topic" %>
   </div>
   <% if user_is_authorized_for_topics? %>
   <div class="col-md-4">
     <%= link_to "New Topic", new_topic_path, class: 'btn btn-success' %>
   </div>
   <% end %>
 </div>
